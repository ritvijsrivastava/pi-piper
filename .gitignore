/target
*.log
.env

# Node (the pi package manifests in package.json / piper-agent/)
node_modules/

# Impeccable working artifacts: ephemeral QA screenshots, not durable
# design documentation (design.json and surfaces/ are kept).
.impeccable/review/
